import React, { useEffect, useState } from 'react'; import axios from './api'; const App = () => { const [items, setItems] = useState([]); const fetchItems = async () => { const res = await axios.get('/items'); setItems(res.data); }; useEffect(() => { fetchItems(); }, []); return (<div><h1>Todo List</h1><ul>{items.map(item => <li key={item._id}>{item.text}</li>)}</ul></div>); }; export default App;